import { NotifierPipe } from './notifer.pipe';

describe('ClassNotififerPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new NotifierPipe();
    expect(pipe).toBeTruthy();
  });
});
